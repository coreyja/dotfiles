# ~/.tmuxinator/retail.yml

name: retail
root: ~/src/better-core

windows:
  - main:
      layout: even-horizontal
      panes:
        - dev
        - cd retail
  - logs:
      layout: main-vertical
      panes:
        - cd retail && powder applog
        - clean-debug
        - cd ../instanbul && git pull --ff-only && ./script/serve.sh
